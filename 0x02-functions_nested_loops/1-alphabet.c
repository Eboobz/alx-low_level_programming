#include <stdio.h>

/**
* main - prints alphabet, followed by a new line
* Return: Always 0 (Success)
*/
void print_alphabet(void)
{
char ch;
ch = 'a';
while (ch <= 'z') 
print_alphabet(ch\n);
return (0);
}
